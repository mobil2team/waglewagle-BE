spring:
  profiles:
    include:
      - secret

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    format_sql: true

  mvc:
    static-path-pattern: /static/**

server:
  port: 80
